%h2
  Locations
  - if @current_role == "admin"
    = link_to 'New Location', new_location_path, class: 'btn btn-primary float-right'
.clearfix
.table-responsive
  %table.table.table-striped.table-hover
    %thead
      %tr
        %th Tenant
        %th Name
        %th Street
        %th House number
        %th Zip
        %th City
        %th State
        %th Country
    %tbody
      - @locations.each do |location|
        %tr
          %td
            = location.tenant.name
          %td
            = location.name
          %td
            = location.street
          %td
            = location.house_number
          %td
            = location.zip_code
          %td
            = location.city
          %td
            = location.state
          %td
            = location.country
          %td
            .d-flex.justify-content-end
              - if @current_role == "admin"
                = link_to 'Edit', edit_location_path(location), class: 'btn btn-sm btn-outline-info mr-2'
                = link_to 'Delete', location_path(location), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-sm btn-outline-danger'
.d-flex.justify-content-center.mt-3
  = paginate @locations