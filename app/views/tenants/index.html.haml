%h2
  Tenants
  - if @current_role == "admin"
    = link_to 'New Tenant', new_tenant_path, class: 'btn btn-primary float-right'
.clearfix
.table-responsive
  %table.table.table-striped.table-hover
    %thead
      %tr
        %th Name
    %tbody
      - @tenants.each do |tenant|
        %tr
          %td
            = tenant.name
          %td
            .d-flex.justify-content-end
              - if @current_role == "admin"
                = link_to 'Edit', edit_tenant_path(tenant), class: 'btn btn-sm btn-outline-info mr-2'
                = link_to 'Delete', tenant_path(tenant), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-sm btn-outline-danger'
.d-flex.justify-content-center.mt-3
  = paginate @tenants